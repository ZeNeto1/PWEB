<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <h1>✅ <%= title %></h1>
  <p>Recebemos seus dados. Obrigado por enviar o formulário!</p>

  <h2>Resumo</h2>
  <ul>
    <li><strong>Nome:</strong> <%= data.nome %></li>
    <% if (data.idade) { %><li><strong>Idade:</strong> <%= data.idade %></li><% } %>
    <% if (data.genero) { %>
      <li><strong>Gênero:</strong>
        <% if (data.genero === '0') { %>Feminino<% } %>
        <% if (data.genero === '25') { %>Masculino<% } %>
        <% if (data.genero === '50') { %>Não-binário<% } %>
        <% if (data.genero === '75') { %>Prefiro não informar<% } %>
        <% if (data.genero === '100') { %>Outro<% } %>
      </li>
    <% } %>
  </ul>

  <p><a href="/componentes">Enviar outro</a> | <a href="/">Voltar à Home</a></p>
</body>
</html>
